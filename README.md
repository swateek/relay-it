# relay-it
An SMS Relay App

## Development

Install [pre-commit](https://pre-commit.com/) and hooks: `pip install pre-commit && pre-commit install`. Hooks run `dart format` and `flutter analyze`; keep the Flutter SDK (and its `dart`) on your `PATH`.
